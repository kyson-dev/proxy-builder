# S-UI 代理服务配置 - Production 环境
# 复制此文件为 .env.production 并填写实际值

# 面板域名（必填）
PANEL_DOMAIN=panel.example.com
DATA_ROOT=${HOME}/data
S_UI_DATA_DIR=${DATA_ROOT}/s-ui
CADDY_DATA_DIR=${DATA_ROOT}/caddy

# 数据根目录（可选，默认 ~/data）
# DATA_ROOT=${HOME}/data
